import { DisplayWhenDirective } from './display-when.directive';

describe('DisplayWhenDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayWhenDirective();
    expect(directive).toBeTruthy();
  });
});
